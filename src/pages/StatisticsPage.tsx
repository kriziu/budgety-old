import { FC } from 'react';
import DatePicker from '../components/DatePicker/DatePicker';

const StatisticsPage: FC = (): JSX.Element => {
  return (
    <div>
      <DatePicker />
    </div>
  );
};

export default StatisticsPage;
